must have:
    - managing tasks 
    - call the nekobot api 
    - storage solution for settings 
can have: 
    - define multiple tasks per user with different intervals
    - maybe set limits on tasks
    - role checks
    - add more interactivity to commands like bot waiting for user input instead of doing simple commands